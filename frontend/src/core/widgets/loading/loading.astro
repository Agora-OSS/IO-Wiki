---
import { Card, CardContent } from "@/core/widgets/card";
import { Loader2 } from "lucide-react";
---
<div slot="fallback" class="min-h-screen bg-background flex items-center justify-center p-4">
  <Card className="w-full max-w-sm">
    <CardContent className="flex flex-col items-center justify-center py-12">
      <Loader2 className="h-12 w-12 animate-spin text-primary mb-4" />
      <h2 class="text-lg font-semibold mb-2">로딩 중...</h2>
      <p class="text-sm text-muted-foreground text-center">페이지 / 위젯을 불러오고 있습니다...</p>
    </CardContent>
  </Card>
</div>